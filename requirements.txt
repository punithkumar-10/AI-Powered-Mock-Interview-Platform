# Streamlit framework for the web app
streamlit>=1.10.0

# Real-time audio/video streaming component for Streamlit
streamlit-webrtc>=0.41.0

# Firebase client library for authentication (pyrebase4 is a maintained fork)
pyrebase4>=4.5.0

# Firebase Admin SDK for Firestore operations
firebase-admin>=6.0.0

# Vosk for offline, real-time speech-to-text
vosk>=0.3.45

# Requests library for HTTP API calls (Gemini API integration)
requests>=2.28.2

# PyAV for audio processing (required by streamlit-webrtc)
av>=10.0.0
